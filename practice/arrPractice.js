arr = ['Leah', 'Belle', 'Miguel', 'Juan'];

for(let i = 0; i < arr.length; i++){
    console.log(arr[i]);
}
/**
 * index starting at 0 will allow it to console log all the names
 * because the length will cover the last one for the less than!
 */

const arrSort = [1, 2, 3, 100, 4, 5];
console.log(arrSort.sort());
// sort will put all the numbers in order based on first digit

const anotherSort = ['Mexico', 'Puerto Rico', 'Colombia', 'Cuba'];
console.log(anotherSort.sort());
// sort will put all the strings in alphabetical order